<!-- ******************* -->
npx what it does?
-> it search for a file and execute it.
-> and that file it could be anything (index.js, abcd.bac,  ravi.md, lokesh.fjks)
<!-- ************ -->
but npm only install the package not execute but npx install and execute both..

<!-- ********************* -->
when you give the shebang in index.js file then you can run with,
npx "main_key".(npx hello).

->
