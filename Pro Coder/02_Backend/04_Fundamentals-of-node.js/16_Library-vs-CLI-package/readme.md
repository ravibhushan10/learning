Library package: axois,express, https (which are used in code by import)

-> when you install then {.bin}  folder is not created in most cases but in some library package are dependent on CLI package so it may {.bin} folder created

<!-- ***************** -->
CLI package : vite (only used by the terminal)

-> Must create {.bin} folder

-> npx vite (to run) -> any CLI package run by "npx"
-> mostly cli package used during development.


<!-- ********* Local and global package ************* -->
local package: the package installed in the node_modules folder

Global package(-g) : it runs from anywhere in the system
