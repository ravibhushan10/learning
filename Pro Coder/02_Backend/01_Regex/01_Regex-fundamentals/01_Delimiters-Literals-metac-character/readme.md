Regex : it is a set of predefined characters.

1: regex101.com
2: regexr.com

<!-- **************** -->
1: delimit -> set the boundary or mark
 eg:  /*     hello     */    -> first and last are set boundary (delimiters)

-> in vs code when we search then with regex the it automatically set the delimiters / / by default..

<!-- ************ -->
2: Literals ->Characters without the special meaning eg: a to z , 0-9

<!-- *************** -->
if global flag is on then it match for the entire code..
if global flag is not on then it match first match and stop..


<!-- ************* -->
3: Meta character ->

.  ->match the any character except the newline(\n) and carrage return (\r)

\  -> its remove the speciality of the any special chracter (\. now it only match the . not the any character and all character).
