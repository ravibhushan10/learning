every process has at least one thread is called main thread , which goes for the execution and we say that process is going to execute.

-> every process run in  main thread

->Ctrl+F : to find the word
->win+h :  to speak and write 

<!-- ********************* -->
To run any terminal inside the any terminal 

bash terminal ->  powershell -Command "dir"
powershell terminal -> bash -Command "ls"
